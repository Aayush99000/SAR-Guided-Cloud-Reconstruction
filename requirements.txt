# =============================================================================
# NOTE: mamba-ssm and causal-conv1d require a CUDA-capable GPU and matching
# CUDA toolkit. If pip install fails, build from source:
#
#   pip install causal-conv1d --no-build-isolation
#   pip install mamba-ssm --no-build-isolation
#
# Or clone and build directly:
#   git clone https://github.com/state-spaces/mamba && cd mamba
#   pip install -e . --no-build-isolation
#
# Requires: CUDA >= 11.6, PyTorch >= 2.0, GCC >= 9
# =============================================================================

# --- Core deep learning ---
# Install PyTorch with CUDA from https://pytorch.org/get-started/locally/
# e.g. pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision>=0.16.0

# --- Mamba SSM (official state-spaces implementation) ---
causal-conv1d>=1.2.0
mamba-ssm>=1.2.0

# --- Tensor manipulation (required by Mamba blocks) ---
einops>=0.7.0

# --- Pretrained vision model utilities ---
timm>=0.9.0

# --- Config management ---
omegaconf>=2.3.0
hydra-core>=1.3.2

# --- Geospatial / Remote sensing I/O ---
# GDAL must be installed at the system level before rasterio:
#   conda: conda install -c conda-forge gdal rasterio
#   apt:   apt-get install gdal-bin libgdal-dev
GDAL>=3.4.0
rasterio>=1.3.0
tifffile>=2023.7.0

# --- Numerical / scientific computing ---
numpy>=1.24.0
scipy>=1.11.0

# --- Image processing & metrics ---
scikit-image>=0.21.0         # SSIM, PSNR, structural metrics
lpips>=0.1.4                 # Learned Perceptual Image Patch Similarity
Pillow>=10.0.0

# --- Experiment tracking ---
wandb>=0.16.0

# --- Visualisation ---
matplotlib>=3.7.0

# --- Utilities ---
tqdm>=4.65.0

# --- Development ---
pytest>=7.4.0
black>=23.0.0
isort>=5.12.0
